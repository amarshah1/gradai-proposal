\section{Results}\label{sec:results}

We investigate how varying the number of provided reward-hacked examples 
and varying the model size affects the evaluation metrics. 
% We also try different prompts for generating unit test reward hacks.

% We use two key metrics to compare the two branches:
% \begin{enumerate}
%   \item \textbf{How frequently the model produces code which satisfies the reward}
%   (percentage of completions which satisfy the reward mechanism, out of the total number of completions).
%   \item \textbf{How frequently the model produces reward hacks, 
%   given that it satisfies the reward}
%   (percentage of incorrect completions, out of the completions which satisfy the reward).
% \end{enumerate}

% We are interested in learning what factors increase both of these percentages;
% in particular, we investigate in-context learning and model size as likely candidates.

% \et{I don't know if we have space for this but a natural story could be showing a pie chart with the baseline results 
% and then motivating our experiments by saying that based on these observations, we wanted to see if we could improve these metrics.}
% We are interested in learning how these metrics vary with the number of reward-hacked few-shot examples
% (in-context learning), and with the size of the model used to generate the code completions.

\subsection{Varying the Percentage of Reward-Hacked Examples} 
This experiment explores in-context learning~\cite{brown2020languagemodelsfewshotlearners}\cite{liu2021pretrainpromptpredictsystematic},
where models iteratively refine their approach based on updated input examples
(rather than changing model parameters). 
Here, we start with the same initial ``bank'' of few-shot examples, 
which contains 9 correct programs and 1 reward hack. 
As we iterate through the completions and tasks, 
% Rather than attempting each task with the same few-shot examples,
each time we encounter a reward hack, we add it to our bank.
Thus, 
although we give the LLM a constant number of 10 few-shot examples throughout the experiment,
later iterations have a higher percentage of reward-hacked few-shot examples in the prompt. 
We hypothesized this would create a greater percentage of reward hacks (metric 2).

\subsection{Varying Model Size} 
In this experiment, we vary the model size from [insert]\etm{todo} to [insert]\etm{todo}, 
keeping the model family and type constant.
We hypothesized that an increased model size would increase the 
percentage of reward-satisfying code completions (metric 1), since using a more model parameters
should make it better able to adapt to Rust and Verus syntax.
\begin{comment}
\subsection{Varying Unit Test Prompt}
Our default prompt for branch A gave the LLM all the unit tests it would be evaluated on, 
but did not explicitly ask it to reward hack. 
We also tested variations where (1) we explicitly asked it to reward hack, but did \emph{not}
give it any of the unit tests (taking inspiration from prior work~\cite{anthropicrh} on reward hacking),
and (2) gave it only some of the unit tests, and evaluated it on a held-out set
(without explicitly asking for a reward hack). 
We hypothesized that we would see a comparable number of reward hacks in (1), 
since the explicit instructions were offset by making it harder to reward hack;
and fewer reward hacks in (2), since this was a strictly harder task.

\paragraph{Setup.} We evaluated Qwen3-Coder on $70$ HumanEval-Verus
benchmarks~\cite{alphaverus}, each consisting of a natural-language description, a
gold-standard Verus specification, and a reference Rust implementation. 
\etm{Duplicate description of benchmark}
For each
problem the model produced $5$ samples (pass@$5$) under each of the two reward
mechanisms described in Section~\ref{intro}: Branch A rewards passing
model-generated unit tests, Branch B rewards verification against a
model-generated Verus specification. Both branches were prompted with $5$
few-shot examples ($4$ correct implementations and $1$ deliberate reward hack,
included so the model is aware reward hacking is on the table). A sample is
labeled \emph{correct} only if it satisfies the gold-standard Verus
specification; this judgement is independent of the in-loop reward signal.

\paragraph{Headline numbers.}
Figure~\ref{fig:results-pass-at-k} summarizes the per-problem outcome
distribution for each branch.

\begin{figure}[h]
\input{figures/results-pass-at-k}
\end{figure}

Under the unit-test reward (Branch A), $70.0\%$ of problems are solved
correctly, but a further $20.0\%$ pass the generated tests while being incorrect
under the gold-standard specification ($11.4\%$ where every one of the $5$
samples is wrong, plus $8.6\%$ where the $5$ samples are a mix of correct and
incorrect implementations that all satisfy the tests). The remaining $10.0\%$
fail the tests outright. We interpret the $20.0\%$ slice as reward hacking:
the model produced code that the reward signal accepts but the
specification rejects. Manual inspection confirms the typical failure
mode is hard-coding the test inputs.

Under the formal-spec reward (Branch B), $21.4\%$ of problems are verified by
Verus and judged correct against the gold-standard, and the remaining $78.6\%$
fail verification. Notably, $0\%$ of samples passed verification while being
incorrect -- the model did not reward-hack. This is the property formal
specifications are supposed to give us: when the reward signal is a
machine-checked proof of a specification, hacking the reward requires producing
a verified-but-wrong program, which the soundness of Verus rules out (modulo
under-specification, which we did not observe in practice on these problems).
\end{comment}

\subsection{Overall Takeaways}
\et{This section might look different after we do our experiments}
\begin{itemize}
\item \textbf{Reward hacking is empirically much rarer with formal specifications.}
  $20.0\%$ vs.\ $0\%$ on this benchmark.
\item \textbf{Reward hacking with formal specifications is not impossible in principle.}
  An under-specification that admits incorrect programs would still be reward
  hackable; we simply did not observe one here.
\item \textbf{Unit tests still produce more \emph{correct} code overall.} Branch
  A reaches $70.0\%$ true success vs.\ $21.4\%$ for Branch B. The bottleneck for
  Branch B is not specification quality but rather the model's ability to
  produce code together with proof annotations that Verus can check; we discuss
  this trade-off further in Section~\ref{challenges}.
\end{itemize}

% \as{Experiments that we want to run next: varying model, A1 vs A2, increasing few-shot, pass@k}
